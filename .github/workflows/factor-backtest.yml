name: factor-backtest

on:
  push:
    branches:
      - analysis/factor-backtest
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --upgrade pip pandas numpy tabulate
      - name: Run core methodology backtest
        run: python analysis/run_methodology_backtest_fast.py
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: methodology-backtest-results-fast
          path: analysis/methodology_results_fast
          if-no-files-found: warn
