name: Factor Regime V1 Research

on:
  push:
    branches:
      - research/factor-lab-v2
    paths:
      - research/factor_regime_v1/run_factor_regime.py
      - research/factor_regime_v1/run_factor_regime_v2.py
      - .github/workflows/factor-regime-v1.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/factor-lab-v2
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --upgrade numpy pandas
      - name: Run Factor Regime v1
        run: python research/factor_regime_v1/run_factor_regime_v2.py
      - name: Commit outputs
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research/factor_regime_v1/results/
          if git diff --cached --quiet; then
            echo "No result changes."
            exit 0
          fi
          git commit -m "Add factor regime v1 results [skip ci]"
          git pull --rebase origin research/factor-lab-v2
          git push origin HEAD:research/factor-lab-v2
