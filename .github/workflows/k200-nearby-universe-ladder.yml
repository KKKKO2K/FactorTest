name: k200-nearby-universe-ladder

on:
  push:
    branches:
      - analysis/k200-nearby-universe-ladder-v1
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ladder:
    strategy:
      fail-fast: false
      matrix:
        add_n: [50, 100, 200, 300]
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      ADD_N: ${{ matrix.add_n }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Overlay latest main factor data
        run: |
          git fetch origin main --depth=1
          git checkout origin/main -- source/factor_all_values
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --upgrade pip pandas numpy tabulate yfinance requests
      - name: Run nearby universe ladder
        run: python analysis/k200_nearby_universe_ladder.py
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: k200-nearby-universe-plus-${{ matrix.add_n }}
          path: analysis/k200_nearby_universe_ladder_results/PLUS_${{ matrix.add_n }}
          if-no-files-found: warn
